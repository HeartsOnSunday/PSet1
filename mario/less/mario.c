#include <stdio.h>
#include <cs50.h>

int main(void)
{
    int height;

    do
    {
       height = get_int("height: ");
    }
    while (height <= 0 || height >= 24);
    {
       //FIX THIS HERE for(i=0; )


        printf("#\n");
    }
}